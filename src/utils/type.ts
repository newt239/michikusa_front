export type Station = {
  name: string;
  latitude: number;
  longitude: number;
};

export type Facility = {
  name: string;
  distance: number;
  color_code: string;
  genre: string;
  latitude: number;
  longitude: number;
};
